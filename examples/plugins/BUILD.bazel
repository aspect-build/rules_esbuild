load("@aspect_rules_esbuild//esbuild:defs.bzl", "esbuild")
load("@esbuild_plugins//:defs.bzl", "link_js_packages")

link_js_packages()

esbuild(
    name = "bundle",
    srcs = ["logo.svg"],
    config = "esbuild.config.mjs",
    entry_point = "main.js",
)
